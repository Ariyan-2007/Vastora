namespace Vastora.Domain.Enums;

public enum ProductStatus
{
    Draft = 1,
    Active = 2,
    OutOfStock = 3,
    Archived = 4
}

public enum DiscountType
{
    Percentage = 1,
    FixedAmount = 2
}
